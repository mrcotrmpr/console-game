#include "database/database.hpp"

//#include "sqlite3.h"

//std::shared_ptr<sqlite> Database::get_connection() const {
//    std::shared_ptr<sqlite> db = nullptr;
//    if (sqlite3_open("huifkar.db", &db) != SQLITE_OK)
//    {
//        sqlite3_close(db);
//        std::cerr << "Error opening database file" << std::endl;
//    }
//    return db;
//}
